#include <stdio.h>

int main() {
    printf("int size: %d байт\n", sizeof(int));
    printf("float size: %d байт\n", sizeof(float));
    printf("double size: %d байт\n", sizeof(double));
    printf("char size: %d байт\n", sizeof(char));
    return 0;
}